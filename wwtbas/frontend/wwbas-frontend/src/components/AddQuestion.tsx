const client = new SuiClient({ url: rpcUrl });
const secretKey = frombase64("mySecretCliKey");
const keyPair = Ed325519keypair.fromSecrectKey(secretKey);
const AddQuestion = () =>{
    async function addQuestion(){
        const tx = new Transsaction()
    } = new Transaction();
}